import React from "react";
import ReactDOM from "react-dom/client";
import LttUI from "./bootstrap";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LttUI />
  </React.StrictMode>,
);
