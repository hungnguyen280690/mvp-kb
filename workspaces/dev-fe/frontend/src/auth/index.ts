export { AuthContext, AuthProvider, type AuthContextType } from './auth-context';
export { useAuth } from './use-auth';
export { MOCK_USERS, getCurrentUser, loginAs, logout, isAuthenticated, hasPermission } from './mock-auth';
