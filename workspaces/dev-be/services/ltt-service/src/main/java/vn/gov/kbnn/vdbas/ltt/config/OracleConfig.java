package vn.gov.kbnn.vdbas.ltt.config;

import org.springframework.context.annotation.Configuration;

/**
 * Oracle config — connection pool, dialect, etc.
 * Oracle 19c voi Oracle JDBC driver (ojdbc11).
 */
@Configuration
public class OracleConfig {
    // Spring Boot auto-configures from application.yml properties
    // Additional bean definitions for Oracle-specific features go here
}
