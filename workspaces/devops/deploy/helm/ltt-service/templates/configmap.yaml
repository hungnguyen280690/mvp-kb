# generated_by: Claude Opus 4.7 (DevOps/SRE)
# applies_adrs: [ADR-0007, ADR-SA-0001, ADR-SA-0002]
# classification: Public
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vdbas.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vdbas.labels" . | nindent 4 }}
  annotations:
    generated_by: "Claude Opus 4.7 (DevOps/SRE)"
data:
  SPRING_PROFILES_ACTIVE: {{ .Values.environment | default "dev" | quote }}
  SAGA_TIMEOUT_SECONDS: {{ .Values.config.saga.timeoutSeconds | quote }}
  SAGA_MAX_RETRIES: {{ .Values.config.saga.maxRetries | quote }}
  OUTBOX_POLLING_INTERVAL_MS: {{ .Values.config.outbox.pollingIntervalMs | quote }}
  OUTBOX_BATCH_SIZE: {{ .Values.config.outbox.batchSize | quote }}
  OUTBOX_RETENTION_DAYS: {{ .Values.config.outbox.retentionDays | quote }}
  COA_VALIDATION_ENABLED: {{ .Values.config.coaValidation.enabled | quote }}
  IDEMPOTENCY_TTL_HOURS: {{ .Values.config.idempotency.ttlHours | quote }}
  LOGGING_FORMAT: {{ .Values.logging.format | default "json" | quote }}
  OTEL_SERVICE_NAME: {{ include "vdbas.name" . }}
